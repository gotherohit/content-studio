# Copy to .env . None of this is required: API keys are added in
# Settings -> Models and keys, and stored in ~/.content-studio/credentials.json.
API_PORT=4700

# Read once on upgrade, to carry an older setup into Settings. Leave it empty.
ANTHROPIC_API_KEY=
